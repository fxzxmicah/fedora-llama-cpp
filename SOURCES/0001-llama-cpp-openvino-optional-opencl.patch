diff --git a/ggml/src/ggml-openvino/CMakeLists.txt b/ggml/src/ggml-openvino/CMakeLists.txt
index af3e0758c..754895ebe 100644
--- a/ggml/src/ggml-openvino/CMakeLists.txt
+++ b/ggml/src/ggml-openvino/CMakeLists.txt
@@ -1,5 +1,6 @@
 find_package(OpenVINO REQUIRED COMPONENTS Runtime Threading)
-find_package(OpenCL REQUIRED)
+find_package(OpenCL QUIET)
+include(CheckIncludeFileCXX)
 
 message(STATUS "Found OpenVINO: ${OpenVINO_DIR} (found version \"${OpenVINO_VERSION}\")")
 
@@ -11,7 +12,22 @@ ggml_add_backend_library(ggml-openvino
     ${GGML_HEADERS_OPENVINO}
 )
 
-target_link_libraries(ggml-openvino PRIVATE openvino::runtime openvino::threading OpenCL::OpenCL)
+target_link_libraries(ggml-openvino PRIVATE openvino::runtime openvino::threading)
+
+if(OpenCL_FOUND)
+    get_target_property(GGML_OPENVINO_RUNTIME_INCLUDE_DIRS openvino::runtime INTERFACE_INCLUDE_DIRECTORIES)
+    set(GGML_OPENVINO_PREV_CMAKE_REQUIRED_INCLUDES "${CMAKE_REQUIRED_INCLUDES}")
+    set(CMAKE_REQUIRED_INCLUDES ${GGML_OPENVINO_RUNTIME_INCLUDE_DIRS})
+    check_include_file_cxx("openvino/runtime/intel_gpu/ocl/ocl.hpp" GGML_OPENVINO_HAVE_INTEL_GPU_OCL_HPP)
+    set(CMAKE_REQUIRED_INCLUDES "${GGML_OPENVINO_PREV_CMAKE_REQUIRED_INCLUDES}")
+    unset(GGML_OPENVINO_PREV_CMAKE_REQUIRED_INCLUDES)
+    unset(GGML_OPENVINO_RUNTIME_INCLUDE_DIRS)
+
+    if(GGML_OPENVINO_HAVE_INTEL_GPU_OCL_HPP)
+        target_compile_definitions(ggml-openvino PRIVATE GGML_OPENVINO_USE_OPENCL)
+        target_link_libraries(ggml-openvino PRIVATE OpenCL::OpenCL)
+    endif()
+endif()
 
 if (GGML_OPENVINO)
     if (CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
diff --git a/ggml/src/ggml-openvino/ggml-openvino-extra.cpp b/ggml/src/ggml-openvino/ggml-openvino-extra.cpp
index 216e3b8a6..5fa7ed9e3 100644
--- a/ggml/src/ggml-openvino/ggml-openvino-extra.cpp
+++ b/ggml/src/ggml-openvino/ggml-openvino-extra.cpp
@@ -5,7 +5,9 @@
 
 #include <cstdlib>
 #include <cstring>
+#ifdef GGML_OPENVINO_USE_OPENCL
 #include <openvino/runtime/intel_gpu/ocl/ocl.hpp>
+#endif
 #include <openvino/runtime/intel_npu/level_zero/level_zero.hpp>
 #include <openvino/runtime/properties.hpp>
 #include <optional>
@@ -106,7 +108,8 @@ void ggml_openvino_device_config::init() {
         compile_config.insert(ov::cache_mode(ov::CacheMode::OPTIMIZE_SIZE));
     }
 
-    // Initialize remote context with queue sharing for GPU
+    // Initialize remote context with queue sharing for GPU when OpenCL interop is available.
+#ifdef GGML_OPENVINO_USE_OPENCL
     if (device_name == "GPU") {
         // Create OpenCL context and queue
         cl_int err;
@@ -146,15 +149,18 @@ void ggml_openvino_device_config::init() {
         // remote tensor is not used for NPU yet
         // remote_context = ov_singleton_core().get_default_context(device_name);
     }
+#endif
 
     initialized = true;
 }

 ggml_openvino_device_config::~ggml_openvino_device_config() {
+#ifdef GGML_OPENVINO_USE_OPENCL
     if (cl_queue != nullptr) {
         clReleaseCommandQueue(cl_queue);
         cl_queue = nullptr;
     }
+#endif
 }
 
 // Get the global device config singleton
@@ -221,6 +227,7 @@ const ov::AnyMap & ggml_openvino_get_compile_config() {
     return ggml_openvino_get_device_config().compile_config;
 }

+#ifdef GGML_OPENVINO_USE_OPENCL
 // Get the OpenCL command queue for GPU operations
 cl_command_queue ggml_openvino_get_cl_queue() {
     return ggml_openvino_get_device_config().cl_queue;
@@ -253,6 +260,7 @@ clEnqueueMemcpyINTEL_fn ggml_openvino_get_clEnqueueMemcpyINTEL() {
     }
     return fn;
 }
+#endif
 
 // Get requantization type for a tensor type (returns nullopt if no requant needed)
 std::optional<ExtraQuantType> ggml_openvino_get_requant_type(const ggml_tensor * tensor, bool no_requant) {
@@ -560,16 +568,20 @@ ggml_openvino_tensor_extra * ggml_openvino_create_tensor_extra(const ggml_tensor
         return nullptr;
     }
 
-    const auto & device_name = ggml_openvino_get_device_name();
-    auto remote_context = ggml_openvino_get_remote_context();
-
     std::shared_ptr<ov::Tensor> ov_tensor;
+#ifdef GGML_OPENVINO_USE_OPENCL
     if (is_remote) {
+        const auto & device_name = ggml_openvino_get_device_name();
+        auto remote_context = ggml_openvino_get_remote_context();
         GGML_ASSERT(device_name == "GPU");
         auto gpu_context = remote_context->as<ov::intel_gpu::ocl::ClContext>();
         auto usm_tensor = gpu_context.create_tensor(element_type, shape, tensor->data);
         ov_tensor = std::make_shared<ov::intel_gpu::ocl::USMTensor>(std::move(usm_tensor));
     } else {
+#else
+    GGML_ASSERT(!is_remote);
+    {
+#endif
         ov_tensor = std::make_shared<ov::Tensor>(element_type, shape, tensor->data);
     }
 
diff --git a/ggml/src/ggml-openvino/ggml-openvino-extra.h b/ggml/src/ggml-openvino/ggml-openvino-extra.h
index 9d827d969..36ebebaaf 100644
--- a/ggml/src/ggml-openvino/ggml-openvino-extra.h
+++ b/ggml/src/ggml-openvino/ggml-openvino-extra.h
@@ -3,9 +3,6 @@
 #include "ggml.h"
 #include "openvino/runtime/core.hpp"
 
-#define CL_TARGET_OPENCL_VERSION 300
-#include <CL/cl.h>
-
 #include <cstdlib>
 #include <memory>
 #include <openvino/core/node.hpp>
@@ -14,6 +11,11 @@
 #include <optional>
 #include <string>
 
+#ifdef GGML_OPENVINO_USE_OPENCL
+#define CL_TARGET_OPENCL_VERSION 300
+#include <CL/cl.h>
+#endif
+
 // ExtraQuantType enum - defines requantization target formats
 // Q4_1_64: u4, group 64, *true* asymmetric (per-group scale and zero point). Note that
 // Q4_0_128/Q4_0_C are symmetric despite taking the unsigned branch of quantize_q4_0 -- that branch
@@ -28,6 +30,7 @@ std::optional<ov::RemoteContext> ggml_openvino_get_remote_context();
 // Get the compile config for the current device
 const ov::AnyMap & ggml_openvino_get_compile_config();
 
+#ifdef GGML_OPENVINO_USE_OPENCL
 // Get the OpenCL command queue for GPU operations (returns nullptr for CPU/NPU)
 cl_command_queue ggml_openvino_get_cl_queue();
 
@@ -55,6 +58,7 @@ clEnqueueMemFillINTEL_fn ggml_openvino_get_clEnqueueMemFillINTEL();
 
 // Get the clEnqueueMemcpyINTEL function pointer (returns nullptr if not available)
 clEnqueueMemcpyINTEL_fn ggml_openvino_get_clEnqueueMemcpyINTEL();
+#endif
 
 // =====================================================
 // Global Device Configuration (singleton)
@@ -68,7 +72,9 @@ struct ggml_openvino_device_config {
     std::optional<ov::RemoteContext> remote_context;
     ov::AnyMap compile_config;
     std::unordered_map<std::string, std::string> environment_variables;
+#ifdef GGML_OPENVINO_USE_OPENCL
     cl_command_queue cl_queue = nullptr;
+#endif
 
     void init();
     ~ggml_openvino_device_config();
diff --git a/ggml/src/ggml-openvino/utils.cpp b/ggml/src/ggml-openvino/utils.cpp
index b1ee792fd..1e4271a98 100644
--- a/ggml/src/ggml-openvino/utils.cpp
+++ b/ggml/src/ggml-openvino/utils.cpp
@@ -29,7 +29,6 @@
 #include <openvino/openvino.hpp>
 #include <openvino/runtime/compiled_model.hpp>
 #include <openvino/runtime/infer_request.hpp>
-#include <openvino/runtime/intel_gpu/ocl/ocl.hpp>
 #include <openvino/runtime/intel_npu/properties.hpp>
 #include <openvino/runtime/properties.hpp>
 #include <openvino/runtime/tensor.hpp>
diff --git a/ggml/src/ggml-openvino/ggml-openvino.cpp b/ggml/src/ggml-openvino/ggml-openvino.cpp
index 02c596223..83f3cdfa4 100644
--- a/ggml/src/ggml-openvino/ggml-openvino.cpp
+++ b/ggml/src/ggml-openvino/ggml-openvino.cpp
@@ -21,7 +21,9 @@
 #include <openvino/core/type/element_type.hpp>
 #include <openvino/openvino.hpp>
 #include <openvino/runtime/allocator.hpp>
+#ifdef GGML_OPENVINO_USE_OPENCL
 #include <openvino/runtime/intel_gpu/ocl/ocl.hpp>
+#endif
 #include <openvino/runtime/intel_npu/level_zero/level_zero.hpp>
 #include <openvino/runtime/tensor.hpp>
 #include <set>
@@ -98,6 +100,7 @@ struct ggml_backend_openvino_buffer_context {
 
         const auto & device_name = ggml_openvino_get_device_name();
 
+#ifdef GGML_OPENVINO_USE_OPENCL
         if (is_remote) {
             GGML_ASSERT(device_name == "GPU");
             auto remote_context = ggml_openvino_get_remote_context();
@@ -106,7 +109,9 @@ struct ggml_backend_openvino_buffer_context {
                 gpu_context.create_usm_device_tensor(ov::element::u8, ov::Shape{size});
             data = usm_tensor.get();
             ov_buffer = std::make_shared<ov::intel_gpu::ocl::USMTensor>(std::move(usm_tensor));
-        } else {
+        } else
+#endif
+        {
 #ifndef _WIN32
             if (const char * spill_dir = ggml_openvino_getenv_str("GGML_OPENVINO_SPILL_DIR")) {
                 // Disk-backed weight buffer: back the repacked weights with a temp file via MAP_SHARED
@@ -285,15 +290,18 @@ static void * ggml_backend_openvino_buffer_get_base(ggml_backend_buffer_t buffer
     return ctx->data;
 }

+#ifdef GGML_OPENVINO_USE_OPENCL
 static bool is_stateful_enabled() {
     return ggml_openvino_getenv_int("GGML_OPENVINO_STATEFUL_EXECUTION") != 0;
 }
+#endif

 static enum ggml_status ggml_backend_openvino_buffer_init_tensor(ggml_backend_buffer_t buffer, ggml_tensor * tensor) {
     // GGML_LOG_DEBUG("%s: buffer usage=%d, tensor name=%s\n", __func__, buffer->usage, tensor->name);
     ggml_backend_openvino_buffer_context * ctx = (ggml_backend_openvino_buffer_context *) buffer->context;
 
-    // Put kvcache on device memory for GPU (NPU memory is too small even for kvcache)
+    // Put kvcache on device memory for GPU when OpenCL interop is available.
+#ifdef GGML_OPENVINO_USE_OPENCL
     if (strncmp(tensor->name, "cache_", 6) == 0 && !ctx->is_remote && ggml_openvino_get_device_name() == "GPU" &&
         !is_stateful_enabled()) {
         GGML_ASSERT(ctx->tensor_extras.empty());
@@ -305,6 +313,7 @@ static enum ggml_status ggml_backend_openvino_buffer_init_tensor(ggml_backend_bu
         buffer->context = ctx;
         tensor->data = (char *) ctx->data + ((char *) tensor->data - (char *) data_prev);
     }
+#endif
 
     // Views share the extra from view_src
     if (tensor->view_src != nullptr) {
@@ -339,8 +348,8 @@ static void ggml_backend_openvino_buffer_memset_tensor(ggml_backend_buffer_t buf
                                                        size_t size) {
     // GGML_LOG_DEBUG("%s: buffer usage=%d, tensor name=%s\n", __func__, buffer->usage, tensor->name);
     GGML_ASSERT(tensor != nullptr && tensor->data != nullptr);
+#ifdef GGML_OPENVINO_USE_OPENCL
     ggml_backend_openvino_buffer_context * ctx = (ggml_backend_openvino_buffer_context *) buffer->context;
-
     if (ctx->is_remote) {
         // For remote (device) buffers, use OpenCL USM memfill
         cl_command_queue queue = ggml_openvino_get_cl_queue();
@@ -356,7 +365,10 @@ static void ggml_backend_openvino_buffer_memset_tensor(ggml_backend_buffer_t buf
         } else {
             GGML_LOG_ERROR("%s: no OpenCL queue or clEnqueueMemFillINTEL not available for GPU buffer\n", __func__);
         }
-    } else {
+    } else
+#endif
+    {
+        GGML_UNUSED(buffer);
         memset((char *) tensor->data + offset, value, size);
     }
 }
@@ -437,6 +449,7 @@ static void ggml_backend_openvino_buffer_set_tensor(ggml_backend_buffer_t buffer
         }
     } else {
         // Non-weight tensor (KV cache, activations, etc.) - copy data. test-backend-ops also goes here
+#ifdef GGML_OPENVINO_USE_OPENCL
         if (ctx->is_remote) {
             cl_command_queue queue = ggml_openvino_get_cl_queue();
             auto mem_cpy_fn = ggml_openvino_get_clEnqueueMemcpyINTEL();
@@ -449,7 +462,9 @@ static void ggml_backend_openvino_buffer_set_tensor(ggml_backend_buffer_t buffer
             } else {
                 GGML_LOG_ERROR("%s: no OpenCL queue or clEnqueueMemcpyINTEL not available for GPU buffer\n", __func__);
             }
-        } else {
+        } else
+#endif
+        {
             memcpy((char *) tensor->data + offset, data, size);
         }
 
@@ -475,8 +490,8 @@ static void ggml_backend_openvino_buffer_get_tensor(ggml_backend_buffer_t buffer
                                                     size_t size) {
     // GGML_LOG_DEBUG("%s: buffer usage=%d, tensor name=%s\n", __func__, buffer->usage, tensor->name);
     GGML_ASSERT(tensor != nullptr && tensor->data != nullptr);
+#ifdef GGML_OPENVINO_USE_OPENCL
     ggml_backend_openvino_buffer_context * ctx = (ggml_backend_openvino_buffer_context *) buffer->context;
-
     if (ctx->is_remote) {
         // For remote (device) buffers, use OpenCL USM memcpy (device-to-host)
         cl_command_queue queue = ggml_openvino_get_cl_queue();
@@ -490,7 +505,10 @@ static void ggml_backend_openvino_buffer_get_tensor(ggml_backend_buffer_t buffer
         } else {
             GGML_LOG_ERROR("%s: no OpenCL queue or clEnqueueMemcpyINTEL not available for GPU buffer\n", __func__);
         }
-    } else {
+    } else
+#endif
+    {
+        GGML_UNUSED(buffer);
         memcpy(data, (const char *) tensor->data + offset, size);
     }
 }
@@ -500,8 +518,8 @@ static bool ggml_backend_openvino_buffer_cpy_tensor(ggml_backend_buffer_t buffer
                                                     ggml_tensor * dst) {
     // GGML_LOG_DEBUG("%s: src tensor name=%s, dst tensor name=%s\n", __func__, src->name, dst->name);
     GGML_ASSERT(src != nullptr && dst != nullptr);
+#ifdef GGML_OPENVINO_USE_OPENCL
     ggml_backend_openvino_buffer_context * ctx = (ggml_backend_openvino_buffer_context *) buffer->context;
-
     if (ctx->is_remote) {
         // For remote (device) buffers, use OpenCL USM memcpy
         cl_command_queue queue = ggml_openvino_get_cl_queue();
@@ -534,6 +552,9 @@ static bool ggml_backend_openvino_buffer_cpy_tensor(ggml_backend_buffer_t buffer
         }
         return false;
     }
+#endif
+
+    GGML_UNUSED(buffer);
 
     // Host buffer - can copy from any host buffer
     if (ggml_backend_buffer_is_host(src->buffer)) {
@@ -546,6 +567,7 @@ static bool ggml_backend_openvino_buffer_cpy_tensor(ggml_backend_buffer_t buffer
 static void ggml_backend_openvino_buffer_clear(ggml_backend_buffer_t buffer, uint8_t value) {
     ggml_backend_openvino_buffer_context * ctx = (ggml_backend_openvino_buffer_context *) buffer->context;
     GGML_ASSERT(ctx->data != nullptr);
+#ifdef GGML_OPENVINO_USE_OPENCL
     if (ctx->is_remote) {
         cl_command_queue queue = ggml_openvino_get_cl_queue();
         auto mem_fill_fn = ggml_openvino_get_clEnqueueMemFillINTEL();
@@ -560,7 +582,9 @@ static void ggml_backend_openvino_buffer_clear(ggml_backend_buffer_t buffer, uin
             GGML_LOG_WARN("%s: no OpenCL queue or clEnqueueMemFillINTEL not available for GPU buffer clear\n",
                           __func__);
         }
-    } else {
+    } else
+#endif
+    {
         memset(ctx->data, value, ctx->size);
     }
 }
